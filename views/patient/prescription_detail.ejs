<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="card shadow-sm p-4">
      <h3 class="text-center text-success mb-4">💊 Chi tiết toa thuốc</h3>

      <div class="mb-3">
        <strong>Bác sĩ:</strong> <%= info.DoctorName %><br>
        <strong>Ngày khám:</strong> <%= info.ScheduleDate ? info.ScheduleDate.toISOString().split('T')[0] : '' %>
      </div>

      <table class="table table-bordered bg-white">
        <thead class="table-success">
          <tr>
            <th>Tên thuốc</th>
            <th>Liều lượng</th>
            <th>Tần suất</th>
            <th>Số ngày</th>
            <th>Ghi chú</th>
          </tr>
        </thead>
        <tbody>
          <% if (items.length === 0) { %>
            <tr><td colspan="5" class="text-center text-muted">Không có thuốc trong toa</td></tr>
          <% } else { %>
            <% items.forEach(it => { %>
              <tr>
                <td><%= it.DrugName %></td>
                <td><%= it.Dosage %></td>
                <td><%= it.Frequency %></td>
                <td><%= it.Days %></td>
                <td><%= it.Note %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>

      <div class="text-center">
        <a href="/patient/prescriptions" class="btn btn-secondary mt-3">⬅️ Quay lại danh sách</a>
      </div>
    </div>
  </div>
</body>
</html>
