<div class="card p-3 shadow-sm">
  <h5 class="mb-3 text-primary">🗓️ Lịch hẹn của tôi</h5>
  <table class="table table-hover align-middle">
    <thead class="table-light">
      <tr>
        <th>Bác sĩ</th>
        <th>Lý do</th>
        <th>Trạng thái</th>
        <th>Đặt lúc</th>
      </tr>
    </thead>
    <tbody>
      <% if (appts && appts.length > 0) { %>
        <% appts.forEach(a => { %>
          <tr>
            <td><%= a.DoctorName %></td>
            <td><%= a.Reason || '-' %></td>
            <td>
              <span class="badge bg-secondary">
                <%= a.Status %>
              </span>
            </td>
            <td>
              <%= new Date(a.BookedAt).toLocaleString('vi-VN') %>
            </td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="4" class="text-center text-muted py-3">
            Chưa có lịch hẹn nào.
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>
