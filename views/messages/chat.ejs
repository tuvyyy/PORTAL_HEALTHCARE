<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .chat-box { max-height: 500px; overflow-y: auto; background: #fff; border-radius: 10px; padding: 15px; }
    .msg { margin-bottom: 10px; }
    .me { text-align: right; }
    .me .bubble { background: #007bff; color: #fff; border-radius: 10px 10px 0 10px; display: inline-block; padding: 8px 12px; }
    .other .bubble { background: #f1f1f1; border-radius: 10px 10px 10px 0; display: inline-block; padding: 8px 12px; }
  </style>
</head>
<body class="bg-light">
<div class="container py-5">
  <h3 class="text-center text-primary mb-3">💬 Trò chuyện</h3>

  <div class="chat-box mb-3">
    <% messages.forEach(m => { %>
      <div class="msg <%= m.SenderId === currentUserId ? 'me' : 'other' %>">
        <div class="bubble">
          <strong><%= m.SenderName %></strong><br>
          <%= m.Content %><br>
          <small class="text-muted"><%= new Date(m.SentAt).toLocaleTimeString() %></small>
        </div>
      </div>
    <% }) %>
  </div>

  <form method="post" action="/messages/<%= threadId %>">
    <div class="input-group">
      <input type="text" name="content" class="form-control" placeholder="Nhập tin nhắn...">
      <button class="btn btn-primary">Gửi</button>
    </div>
  </form>

  <div class="mt-3 text-center">
    <a href="/messages" class="btn btn-outline-secondary">⬅️ Quay lại</a>
  </div>
</div>
</body>
</html>
