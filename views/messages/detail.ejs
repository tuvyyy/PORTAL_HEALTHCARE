<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Hội thoại</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .chat-box {
      height: 400px;
      overflow-y: auto;
      background: #fff;
      border-radius: 8px;
      padding: 10px;
      box-shadow: 0 0 5px rgba(0,0,0,0.1);
    }
    .msg-outgoing {
      text-align: right;
    }
    .msg-incoming {
      text-align: left;
    }
    .msg-bubble {
      display: inline-block;
      padding: 8px 12px;
      border-radius: 16px;
      margin: 4px 0;
      max-width: 70%;
    }
    .msg-outgoing .msg-bubble {
      background-color: #007bff;
      color: white;
    }
    .msg-incoming .msg-bubble {
      background-color: #f1f1f1;
      color: #333;
    }
  </style>
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="text-primary mb-3">💬 Tin nhắn trong hội thoại</h2>

    <div class="chat-box mb-3">
      <% if (messages && messages.length > 0) { %>
        <% messages.forEach(msg => { %>
          <div class="<%= msg.Direction === 'outgoing' ? 'msg-outgoing' : 'msg-incoming' %>">
            <div class="msg-bubble">
              <strong><%= msg.SenderName %>:</strong> <%= msg.Content %><br>
              <small class="text-muted"><%= new Date(msg.SentAt).toLocaleString() %></small>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="text-center text-muted mt-3">Không có tin nhắn nào trong hội thoại này.</p>
      <% } %>
    </div>

    <form method="POST" action="/messages/<%= messages.length > 0 ? messages[0].ThreadId : '' %>" class="d-flex">
      <input type="text" name="content" placeholder="Nhập tin nhắn..." class="form-control me-2" required />
      <button type="submit" class="btn btn-primary">Gửi</button>
    </form>

    <div class="mt-3">
      <a href="/messages" class="btn btn-secondary">⬅️ Quay lại danh sách hội thoại</a>
    </div>
  </div>
</body>
</html>
