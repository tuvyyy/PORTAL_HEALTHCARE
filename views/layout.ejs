<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Trang bệnh nhân' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    .sidebar {
      height: 100vh;
      background-color: #ffffff;
      border-right: 1px solid #dee2e6;
      padding: 20px;
    }
    .sidebar a {
      display: block;
      color: #333;
      padding: 10px 15px;
      border-radius: 6px;
      text-decoration: none;
      margin-bottom: 5px;
      font-weight: 500;
    }
    .sidebar a.active, .sidebar a:hover {
      background-color: #0d6efd;
      color: white;
    }
  </style>
</head>
<body>
  <div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar">
      <h5 class="text-primary mb-4">🧍 Bệnh nhân</h5>
      <a href="/patient/profile" class="<%= active === 'profile' ? 'active' : '' %>">👤 Hồ sơ cá nhân</a>
      <a href="/patient/appointments" class="<%= active === 'appointments' ? 'active' : '' %>">🗓️ Lịch khám</a>
      <a href="/patient/records" class="<%= active === 'records' ? 'active' : '' %>">📋 Hồ sơ khám</a>
      <a href="/patient/prescriptions" class="<%= active === 'prescriptions' ? 'active' : '' %>">💊 Toa thuốc</a>
      <a href="/messages" class="<%= active === 'messages' ? 'active' : '' %>">💬 Tin nhắn</a>

      <hr>
      <form method="POST" action="/logout">
        <button class="btn btn-outline-danger w-100">🚪 Đăng xuất</button>
      </form>
    </div>

    <!-- Nội dung -->
    <div class="flex-grow-1 p-4">
      <%- body %>
    </div>
  </div>
</body>
</html>
