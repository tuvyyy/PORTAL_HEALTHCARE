<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="card shadow-sm p-4">
      <h3 class="text-center text-primary mb-4">✏️ Sửa hồ sơ khám</h3>

      <div class="mb-3">
        <strong>Bệnh nhân:</strong> <%= record.PatientName %><br>
        <strong>Ngày khám:</strong> <%= record.ScheduleDate ? record.ScheduleDate.toISOString().split('T')[0] : '' %>
      </div>

      <form method="POST" action="/doctor/record/edit/<%= record.RecordId %>">
        <div class="mb-3">
          <label class="form-label">Tóm tắt (Summary)</label>
          <textarea name="summary" class="form-control" rows="2" required><%= record.Summary || '' %></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label">Chẩn đoán (Diagnosis)</label>
          <textarea name="diagnosis" class="form-control" rows="2"><%= record.Diagnosis || '' %></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label">Ghi chú (Note)</label>
          <textarea name="note" class="form-control" rows="3"><%= record.Note || '' %></textarea>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/doctor/appointments" class="btn btn-secondary">⬅️ Quay lại</a>
          <button class="btn btn-success">💾 Lưu thay đổi</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
