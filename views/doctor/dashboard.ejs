<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Trang bác sĩ</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen bg-gray-100 flex font-sans text-gray-800">

  <!-- Main content -->
  <main class="flex-1 p-8">
    <!-- Header -->
    <header class="flex items-center justify-between mb-6">
      <div>
        <h2 class="text-2xl font-semibold">Xin chào, <%= currentUser?.FullName || 'Bác sĩ' %></h2>
        <p class="text-sm text-gray-500">Hôm nay: <%= today %></p>
      </div>
      <div class="w-12 h-12 rounded-full bg-gray-300 flex items-center justify-center text-xl">
        👨‍⚕️
      </div>
    </header>

    <!-- Card danh sách bệnh nhân -->
    <section class="bg-white rounded-lg shadow p-6">
      <h3 class="text-lg font-semibold mb-4">Danh sách bệnh nhân trong ca</h3>

      <div class="overflow-x-auto">
        <table class="min-w-full border text-sm">
          <thead class="bg-gray-100">
            <tr>
              <th class="border px-4 py-2 text-left">#</th>
              <th class="border px-4 py-2 text-left">Tên bệnh nhân</th>
              <th class="border px-4 py-2 text-left">Ngày khám</th>
              <th class="border px-4 py-2 text-left">Ghi chú</th>
            </tr>
          </thead>
          <tbody>
            <% if (appointments && appointments.length > 0) { %>
              <% appointments.forEach((p, i) => { %>
                <tr class="hover:bg-gray-50 transition">
                  <td class="border px-4 py-2"><%= i + 1 %></td>
                  <td class="border px-4 py-2 font-medium"><%= p.PatientName %></td>
                  <td class="border px-4 py-2"><%= p.ScheduleDate %></td>
                  <td class="border px-4 py-2"><%= p.Reason %></td>
                </tr>
              <% }) %>
            <% } else { %>
              <tr>
                <td colspan="4" class="text-center py-6 text-gray-500 italic">
                  Hiện chưa có bệnh nhân nào trong ca hôm nay.
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
  </main>

</body>
</html>
