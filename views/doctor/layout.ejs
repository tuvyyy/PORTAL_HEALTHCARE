<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Cổng thông tin bác sĩ' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f5f8f6; }
    .sidebar {
      height: 100vh;
      width: 240px;
      background-color: #ffffff;
      border-right: 1px solid #dcdcdc;
      padding: 20px;
    }
    .sidebar h5 {
      color: #198754;
      font-weight: 700;
      margin-bottom: 25px;
    }
    .sidebar a {
      display: block;
      color: #333;
      text-decoration: none;
      padding: 10px 15px;
      border-radius: 8px;
      margin-bottom: 5px;
      transition: background 0.2s;
      font-weight: 500;
    }
    .sidebar a.active, .sidebar a:hover {
      background-color: #198754;
      color: white;
    }
    .content {
      flex-grow: 1;
      padding: 30px;
      overflow-y: auto;
    }
    .logout-btn {
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="d-flex">
    <!-- Sidebar -->
    <div class="sidebar shadow-sm">
      <h5>👨‍⚕️ Bác sĩ</h5>
      <a href="/doctor" class="<%= active === 'dashboard' ? 'active' : '' %>">📊 Trang chính</a>
      <a href="/doctor/appointments" class="<%= active === 'appointments' ? 'active' : '' %>">🗓️ Lịch khám</a>
      <a href="/messages" class="<%= active === 'messages' ? 'active' : '' %>">💬 Tin nhắn</a>
      <hr>
      <form method="POST" action="/logout">
        <button class="btn btn-outline-danger w-100 logout-btn">🚪 Đăng xuất</button>
      </form>
    </div>

    <!-- Main Content -->
    <div class="content">
      <%- body %>
    </div>
  </div>
</body>
</html>
